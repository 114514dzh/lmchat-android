# 复制为 local.properties 后填入自己的值（local.properties 不入库）
# 只需填这一处服务器地址，更新地址会自动从它推导：
#   https://host/im-api  ->  更新清单 https://host/im-update/version.json
server=https://your-server.example/im-api
keystore.pass=change-me
